services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: qosh
      POSTGRES_PASSWORD: qosh_secret
      POSTGRES_DB: qosh_db
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: