version: '3.8'
services:
  db:
    image: postgres:18
    environment:
      POSTGRES_USER: qosh
      POSTGRES_PASSWORD: qosh_secret
      POSTGRES_DB: qosh_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: